import * as React from 'react';
import './Teams.css';

const Teams = (props: any) => {
  return (
    <div className="teams-container">
      <h1>My teams</h1>
    </div>
  );
};

export default Teams;
